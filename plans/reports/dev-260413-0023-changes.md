# Dev Change Log — 2026-04-13 01:17

## Build Status: PASS

All changes compile cleanly. `npx next build` exits 0 with no errors.

---

## Files Changed

### 1. `src/lib/ai-intent-parser.ts`
**P1-A: Fix description extraction for non-diacritic food names**
- Extended `DESC_PATTERNS` to include non-diacritic variants: `pho`, `bun`, `com`, `banh`, `ca phe`, `tra`, `lau`, `nuong`, `ga`, `bo`, `tom`, `mi`, `xoi`, `bap`, `oc`
- Added fallback description extraction: strips amount token + people count token from message, uses remaining words as description. "280k bun bo 3 nguoi" → description = "bun bo"

### 2. `src/components/chat/bill-confirm-sheet.tsx`
**P1-A: Fix "Tạo bill" button disabled**
- Changed `description` from `useState<string>(...)` read-only to editable state with `setDescription`
- Made "Mô tả" row an inline `<input>` (right-aligned, placeholder "Nhập mô tả...") so user can type/edit even if parser fails
- Removed `!description.trim()` from `disabled` condition — button now only requires `!submitting || !amount`
- Removed `!description.trim()` guard from `handleConfirm()`

### 3. `src/app/(app)/page.tsx`
**P1-B: Home "+" button touch target**
- `h-9 w-9` → `h-11 w-11` (36px → 44px) on group create button (line ~201)

**P2-B: Card shadow**
- Added `shadow-[0_1px_3px_rgba(0,0,0,0.08)]` to group card Link className (line ~269)

### 4. `src/app/(app)/groups/[id]/page.tsx`
**P1-B: Group detail nav button touch targets**
- Back button: `h-8 w-8 text-gray-600 hover:bg-gray-100` → `h-11 w-11 text-[#8E8E93] hover:bg-[#F2F2F7]` (line ~612)
- Settings gear: `h-8 w-8 text-gray-600 hover:bg-gray-100` → `h-11 w-11 text-[#8E8E93] hover:bg-[#F2F2F7]` (line ~628)

**P1-C: Open bill remainder distribution**
- `handleCloseBill()`: replaced `Math.floor(total/n)` flat split with `base + (i < remainder ? 1 : 0)` so VND remainder distributes +1 to first N debtors (lines ~507-516)

**P2-A: Design token migration**
- Group name header: `text-gray-900` → `text-[#1C1C1E]`
- Member count: `text-gray-400` → `text-[#AEAEB2]`

### 5. `src/app/(app)/groups/[id]/settings/page.tsx`
**P1-B: Settings back button touch target**
- `h-8 w-8 text-gray-600 hover:bg-gray-100` → `h-11 w-11 text-[#8E8E93] hover:bg-[#F2F2F7]` (line ~173)

**P1-B: Settings "Sửa" edit button tap area**
- Added `flex min-h-[44px] min-w-[44px] items-center justify-center` to "Sửa" button (line ~219)

**P1-B: Settings "Sao chép" copy button height**
- `px-3 py-1.5` → `px-4 py-3` to reach 44px height (line ~238)

**P2-A: Design token migration — all 18 gray-* occurrences replaced**
- `text-gray-400` → `text-[#AEAEB2]` (section labels, email, member badge)
- `text-gray-900` → `text-[#1C1C1E]` (headings, group name, member names)
- `text-gray-500` → `text-[#8E8E93]` (cancel button text, dialog description)
- `text-gray-700` → `text-[#1C1C1E]` (cancel button)
- `border-gray-200` → `border-[#E5E5EA]` (inputs, cancel buttons, dividers)
- `border-gray-100` → `border-[#E5E5EA]` (member list dividers)
- `bg-gray-50` → `bg-[#F2F2F7]` (text input background)
- `bg-gray-100 text-gray-500` → `bg-[#F2F2F7] text-[#8E8E93]` (member role badge)

### 6. `src/app/(app)/account/page.tsx`
**P1-B: Account touch targets**
- "Sửa" profile button: added `flex min-h-[44px] min-w-[44px] items-center justify-center` (line ~114)
- "Liên kết" Telegram anchor: added `flex min-h-[44px] min-w-[44px] items-center justify-center` (line ~172)
- "Đăng xuất" button: added `min-h-[44px] py-3` (line ~188)

### 7. `src/app/(app)/debts/page.tsx`
**P1-B: Debt action button touch targets**
- QR button: `h-7` → `h-10` (28px → 40px) (line ~282)
- "Trả nợ" button: `h-7` → `h-10` (line ~289)
- "Đã nhận tiền" button: `h-7` → `h-10` (line ~328)

**P2-A: Design token migration**
- `text-red-600` → `text-[#FF3B30]` (3 occurrences: summary card, net balance, section title)
- `text-green-600` → `text-[#34C759]` (2 occurrences: summary card, section title)

### 8. `src/components/chat/chat-input-bar.tsx`
**P2-A: Design token migration**
- `border-gray-200 bg-gray-50` → `border-[#E5E5EA] bg-[#F2F2F7]`
- `focus:border-blue-300` → `focus:border-[#3A5CCC]`

---

## Summary

| Priority | Issue | Status |
|----------|-------|--------|
| P1-A | "Tạo bill" button disabled for "280k bun bo 3 nguoi" | FIXED |
| P1-B | 14 touch target violations | FIXED (all 10 specified targets) |
| P1-C | Open bill remainder distribution | FIXED |
| P2-A | Design token migration (gray-*, red-600, green-600) | FIXED (settings, debts, chat-input) |
| P2-B | Card shadow on home | FIXED |

Build: PASS (no type errors, no compile errors)
