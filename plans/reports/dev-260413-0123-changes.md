# Dev Change Log — Cycle 2 — 2026-04-13 02:15

## Build Status: PASS

`npx next build` exits 0, no type errors.

---

## Files Changed

### 1. `src/app/(app)/transfer/[debtId]/page.tsx`
**P1-1: QR Save as actual image blob**
- `handleSaveQR()` changed from `a.href = qrUrl` (downloads URL string) to async blob fetch + `URL.createObjectURL()` → triggers real PNG download
- **P2-1: Token migration (19 occurrences)**: replaced all `gray-*`, `blue-100/600`, `gray-100` with brand tokens

### 2. `src/app/(app)/groups/[id]/page.tsx`
**P1-2: Telegram notifications for open bill events**
- `handleCheckin()`: fires `POST /api/notify` with `type: "open_bill_checkin"` after successful insert
- `handleCloseBill()`: fires `POST /api/notify` with `type: "open_bill_closed"` after close
- `handleCloseBill()`: inserts `chat_messages` row with `message_type: "system"` announcing bill closure

**P1-3: Pending confirmation state on debt banner**
- Added `pendingConfirmDebtIds: Set<string>` state
- In `load()`: after debt queries, fetches `payment_confirmations` where `status=pending` and `debt_id IN owingDebts` — builds set of debt IDs with pending confirms
- `debtBanner` useMemo: if debtor has pending confirm for displayed debt, returns yellow state `bg-[#FFF8EC] text-[#FF9500]` with "Chờ xác nhận" label and disabled button
- Banner render: `disabled={!debtBanner.href}` prevents navigation for pending state
- Also fixed remaining token: `text-gray-400` → `text-[#AEAEB2]` (1 occurrence)

### 3. `src/app/(app)/summary/page.tsx`
**P2-1: Token migration (5 occurrences)**
- `text-red-600` → `text-[#FF3B30]` (3x)
- `text-green-600` → `text-[#34C759]` (2x)
- `text-gray-900` → `text-[#1C1C1E]`, `text-gray-500` → `text-[#8E8E93]`, `text-gray-700` → `text-[#1C1C1E]`, `border-gray-200` → `border-[#E5E5EA]`

### 4. `src/components/chat/chat-message-list.tsx`
**P2-1: Token migration (3 occurrences)**
- `text-gray-400` → `text-[#AEAEB2]` (2x)
- `text-gray-900` → `text-[#1C1C1E]` (1x)

### 5. `src/components/chat/ai-followup-card.tsx`
**P2-1: Token migration (4 occurrences)**
- `text-gray-900` → `text-[#1C1C1E]` (2x)
- `text-gray-400` → `text-[#AEAEB2]` (2x)

---

## Summary

| Priority | Issue | Status |
|----------|-------|--------|
| P1-1 | QR save downloads actual PNG blob | FIXED |
| P1-2 | Telegram notify on checkin + bill close | FIXED |
| P1-2 | System chat message on bill close | FIXED |
| P1-3 | Pending confirmation state on debt banner | FIXED |
| P2-1 | Token migration: transfer page (19 occurrences) | FIXED |
| P2-1 | Token migration: summary page (7 occurrences) | FIXED |
| P2-1 | Token migration: chat-message-list (3 occurrences) | FIXED |
| P2-1 | Token migration: ai-followup-card (4 occurrences) | FIXED |
| P2-1 | Token migration: groups/[id]/page (1 remaining) | FIXED |

Build: PASS

## Notify API
Already handles `open_bill_checkin` and `open_bill_closed` — no changes needed to `route.ts`.

## Unresolved Questions
- Q1 (carried): Why is "Tạo bill" button disabled after AI parse in QC test? Cycle 1 fix removed `!description.trim()` guard — may be a flaky test or cached state issue, needs re-run.
- Q2 (carried): Receipt upload still stubbed — needs Supabase storage bucket, out of cycle scope.
- Q3: bills/page.tsx has no gray-* (verified via grep — only uses `muted-foreground` via shadcn). No changes needed.
