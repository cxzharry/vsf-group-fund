# Dev Changes Report — Cycle 3 — 2026-04-13 03:01

## Status: Complete

---

## P1: Edit/Delete Bills Feature

### Files Modified

**`src/components/chat/bill-card-bubble.tsx`**
- Added `"use client"` directive + `useState` import (needed for menu toggle)
- Added `onDelete?: (billId: string) => void` prop
- Added `isOwner` check (`bill.paid_by === currentMemberId`)
- Added 3-dot menu button (only rendered when `isOwner && onDelete`)
- Menu contains: "Sửa bill" stub (shows tooltip "Sắp có") + "Xóa bill" (calls `onDelete`)
- Menu closes via backdrop click
- Token migration: all 12 raw gray-* tokens replaced with design tokens

**`src/components/chat/chat-message-list.tsx`**
- Added `onDeleteBill?: (billId: string) => void` to `ChatMessageListProps`
- Passed `onDelete={onDeleteBill}` to `BillCardBubble`

**`src/app/(app)/groups/[id]/page.tsx`**
- Added `deleteBillId` state (confirmation dialog control)
- Added `handleDeleteBill(billId)` function:
  1. Guards: `bill.paid_by !== currentMember.id` → error toast
  2. Deletes `debts` where `bill_id = billId`
  3. Deletes `bill_participants` where `bill_id = billId`
  4. Deletes `bill_checkins` where `bill_id = billId`
  5. Deletes `bills` row
  6. Deletes `chat_messages` with `metadata.bill_id = billId`
  7. Updates local state: removes bill + related chat messages
  8. Shows toast "Đã xóa bill"
- Wired `onDeleteBill={(billId) => setDeleteBillId(billId)}` on `ChatMessageList`
- Added confirmation dialog overlay: "Xóa bill? Thao tác này không thể hoàn tác."
  - Cancel → closes dialog
  - "Xóa bill" (red) → calls `handleDeleteBill`

---

## P2: Token Migration

### `src/components/chat/add-people-sheet.tsx`
16 tokens → 0. All replaced:
- `bg-gray-300` → `bg-[#C7C7CC]`
- `text-gray-900` → `text-[#1C1C1E]`
- `bg-gray-100 text-gray-500` → `bg-[#F2F2F7] text-[#8E8E93]`
- `border-gray-200 bg-gray-50` → `border-[#E5E5EA] bg-[#F2F2F7]`
- `text-gray-400` → `text-[#AEAEB2]`
- `bg-blue-100 text-blue-600` → `bg-[#EEF2FF] text-[#3A5CCC]`
- `text-green-600` → `text-[#34C759]`
- `border-gray-300 text-gray-500 active:bg-gray-50` → `border-[#C7C7CC] text-[#8E8E93] active:bg-[#F2F2F7]`

### `src/components/chat/bill-card-bubble.tsx`
12 tokens → 0 (migrated during P1 work):
- `text-gray-500` → `text-[#8E8E93]`
- `text-gray-900` → `text-[#1C1C1E]`
- `border-gray-100` → `border-[#E5E5EA]`
- `text-gray-400` → `text-[#AEAEB2]`
- `bg-gray-100 text-gray-400` → `bg-[#F2F2F7] text-[#AEAEB2]`

### `src/components/chat/open-bill-card.tsx`
7 tokens → 0:
- `text-gray-900` → `text-[#1C1C1E]` (×2)
- `text-gray-700` → `text-[#636366]`
- `text-gray-400` → `text-[#AEAEB2]` (×2)
- `bg-green-50 text-green-600 border-green-200` → `bg-[#F0FFF4] text-[#34C759] border-[#34C759]/30`

---

## Build Verification

`npx next build` → **success**, 0 type errors, 0 compile errors
Pre-existing ESLint warnings unchanged (not introduced by this cycle).

---

## Token Counts (Updated)

App-wide raw tokens: 51 → **16** (35 cleared this cycle = 69% reduction of remaining)

Remaining files:
| File | Count |
|---|---|
| components/chat/ai-response-card.tsx | 6 |
| app/(app)/debts/page.tsx | 6 |
| app/(app)/summary/page.tsx | 2 |
| components/chat/date-divider.tsx | 1 |
| components/screenshot-upload.tsx | 1 |

---

## Deferred

- **Edit bill** (P1): stub menu item shows "Sắp có" label. Full implementation deferred — requires BillConfirmSheet edit-mode prop, debt mutation logic.
- Telegram notify on delete: out of scope this cycle per task brief.

## Unresolved Questions
- None.
