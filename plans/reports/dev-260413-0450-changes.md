# Dev Report — Simplify Debts (Pairwise Netting v1)
Date: 2026-04-13 04:57

## Files Modified

| File | Action | Lines |
|------|--------|-------|
| `src/lib/simplify-debts.ts` | Created | 45 |
| `src/lib/__tests__/simplify-debts.test.ts` | Created | 65 |
| `src/app/(app)/debts/page.tsx` | Modified | +150 |

## Summary

### Algorithm (`src/lib/simplify-debts.ts`)
- Pure function `simplifyDebts(debts: RawDebt[]): SimplifiedDebt[]`
- Groups debts by unordered pair key `smallerId|largerId`
- Nets A→B vs B→A; drops zero-net pairs; returns `underlying_ids` for batch settle
- No schema change — 100% client-side

### Tests (`src/lib/__tests__/simplify-debts.test.ts`)
6 cases: empty, single debt, partial net (100-30=70), full net (zeroed), multi-person mixed, same-direction sum.
All 6 pass.

### Debts Page (`src/app/(app)/debts/page.tsx`)
- `viewMode: 'detail' | 'simplified'` state, default `'detail'`
- Segmented toggle: `[Chi tiết] [Nợ ròng]` — active = `bg-[#3A5CCC] text-white`, inactive = `text-[#8E8E93]`
- Simplified mode: shows netted summary cards, I-owe and owed-to-me sections with chip `"Nợ ròng · gộp N"`
- Empty simplified state: "🎉 Tất cả đã cân bằng!"
- `handleBatchConfirmPaid` / `handleBatchCreditorConfirm` settle all underlying debt ids atomically via `.in('id', [...])`
- `getMemberName()` helper resolves member display name from enriched debts array
- Detail mode: unchanged from before

## Build & Tests
- `npx next build`: PASS (0 new errors, pre-existing warnings only)
- `npx jest simplify-debts`: 6/6 PASS

## Unresolved Questions
- None for v1 scope. Graph-based multi-hop netting deferred to cycle 6 per PRD.
