# PRD Review — 2026-04-13 01:50 (Cycle 2)

## Delta from Cycle 1

Cycle 1 fixed: AI parser + editable description, 14 touch targets (44px+), open bill remainder +1, 26 design token migrations, card shadows. All verified in code.

### 3 PARTIAL Stories — Status Check

| US | Issue | Cycle 1 Fix? | Still Missing |
|----|-------|-------------|---------------|
| 3.4 Open Bill | Remainder +1 | YES (L507-511 base+remainder pattern) | Telegram notify on checkin/close, chat message on close |
| 3.5 Transfer | QR save blob | NO — `handleSaveQR()` L134-140 still `a.href = qrUrl` (URL, not blob) | QR save blob, share sends URL not image, receipt upload stubbed |
| 4.3 Payment Confirm | Pending banner | NO — no "Chờ xác nhận" / pending state in debts page | Pending confirmation banner, debtor Telegram notify |

**No PARTIAL→DONE upgrades this cycle.** Only US-E3-4 remainder sub-issue resolved.

## Gap Matrix (Updated)

| US | Name | Status | Delta |
|----|------|--------|-------|
| 1.1–1.5 | Login/Onboarding/Branding | DONE | — |
| 2.1–2.5 | Groups (list/create/join/detail/settings) | DONE | — |
| 3.1 | AI Parser | DONE | — |
| 3.2 | Bill Confirm Sheet | DONE | — |
| 3.3 | Split Sheet | DONE | — |
| 3.4 | Open Bill | PARTIAL | Remainder fixed; Telegram + chat msg still missing |
| 3.5 | Transfer/Payment | PARTIAL | No change from last review |
| 4.1 | Debt Banner | DONE | Tokens fixed (red→#FF3B30, green→#34C759) |
| 4.2 | Net Debt Calc | DONE | — |
| 4.3 | Payment Confirm 2-way | PARTIAL | No change from last review |
| 5.1–5.5 | Account pages | DONE | — |

**Totals: DONE 16 | PARTIAL 3 | MISSING 0** (unchanged)

## Remaining Work — Prioritized

1. **P2 — US-E3-5 QR save as blob** — `transfer/[debtId]/page.tsx` L134: fetch qrUrl as blob, create objectURL. Share should use `navigator.share({files})` not `{url}`.
2. **P2 — US-E3-4 Telegram notify on checkin/close** — `handleCheckin()` and `handleCloseBill()` in `groups/[id]/page.tsx` don't call any notify function. `notifyNewBill` exists in `lib/notifications.ts` but no `notifyCheckin`/`notifyBillClose`. Need new notify helpers + API route handler.
3. **P2 — US-E3-4 Chat message on bill close** — `handleCloseBill()` L484-533 updates DB + local state but never inserts a chat message. Should insert system message like "Bill [desc] đã đóng".
4. **P2 — US-E4-3 Pending confirmation banner** — Debts page has no query for `payment_confirmations` where `status=pending`. Need yellow "Chờ xác nhận" banner state between "Trả nợ" and "Đã nhận".
5. **P3 — US-E3-5 Receipt upload** — Chat input has receipt icon but no upload handler. Still stubbed.
6. **P3 — ~67 remaining gray-* tokens** in files not touched by cycle 1.

## Unresolved Questions

1. Are extra pages (/bills, /debts list, /activity, /summary, /members) intentional scope or tech debt? (carried from cycle 1)
2. Should "Nhắc nợ" trigger actual Telegram reminder? Currently just navigates.
3. Receipt upload (US-E3-5): needs Supabase storage bucket + upload API — is this in scope for MVP?
