# Design Spec — `<slug>-design.md`

> Markdown design specification — single source of truth for product design. Replaces "tokens.json only" output. Generated by `ui-ux-designer` agent. Consumed by `/team-prototype`, `/team-vibe`, `/team-uat`, and Dev handoff.
>
> Lives at: `designs/<slug>-design.md`. Pairs with `designs/<slug>.pen` (visual file, when Pencil available) and `design-system/tokens.json` (machine-readable tokens, derived from this doc).

---

## 1. Style Decision

- **Chosen style:** [from `templates/design-styles.md` catalog OR custom]
- **Rationale:** [1-3 sentences — why this style fits the persona + product type]
- **PO confirmation:** [date PO approved via AskUserQuestion]
- **Reference link to style file:** `designs/<slug>-style.md`

## 2. Design Tokens

> Same data as `design-system/tokens.json` — duplicated here in human-readable form so Dev can review without parsing JSON.

### 2.1 Color
- **Primary:** `#xxxxxx` (use cases: ...)
- **Secondary:** `#xxxxxx`
- **Accent:** `#xxxxxx`
- **Background:** `#xxxxxx` (light) / `#xxxxxx` (dark if applicable)
- **Text:** `#xxxxxx` (primary) / `#xxxxxx` (muted)
- **Semantic:** success `#xxxxxx`, warn `#xxxxxx`, danger `#xxxxxx`

### 2.2 Typography
- **Display:** [font name, weight] — e.g. `Inter, 700` (headings)
- **Body:** [font name, weight] — e.g. `Inter, 400`
- **Mono:** [font name] — for code/data
- **Vietnamese support:** [yes/no, fallback if needed]

### 2.3 Spacing scale
- Base unit: `4px`
- Scale: `4 / 8 / 12 / 16 / 24 / 32 / 48 / 64`

### 2.4 Radius / Shadow
- Radius: `0 / 4 / 8 / 16 / 9999`
- Shadow tiers: sm / md / lg with values

### 2.5 Motion
- Easings, durations, prefers-reduced-motion handling

## 3. Screen Inventory

> One row per screen. Cap = 7 screens MVP (or as PRD scope dictates). For `--scope=skeleton` design, list only key screens (1 per epic).

| ID | Screen | Epic | Purpose | States covered |
|---|---|---|---|---|
| S1 | [name] | E1 | [purpose] | default, loading, empty, error |
| S2 | ... | E2 | ... | default |

## 4. Component Inventory

> Reusable components used across screens. Map to shadcn/ui primitives where possible.

| Component | Used in screens | Variants | Notes |
|---|---|---|---|
| Button | S1, S3, S5 | primary, secondary, ghost, destructive | `size: sm/md/lg` |
| Card | S2, S4 | default, interactive | optional `header` slot |
| ... | ... | ... | ... |

## 5. Per-Screen Specs

### Screen S1 — [Name]

- **Purpose:** [1 line]
- **Persona target:** [`personas/<slug>.md`]
- **Layout:** [grid description, breakpoints]
- **Components:** S1 uses [Button.primary, Card.interactive, ...]
- **States:**
  - **Default:** [what shows]
  - **Loading:** [skeleton / spinner placement]
  - **Empty:** [empty state message + CTA]
  - **Error:** [error UI + retry]
- **Interactions:** [hover, focus, click → navigation, etc.]
- **Acceptance link:** maps to PRD `US-E1-1` AC items

### Screen S2 — [Name]
... (same structure)

## 6. User Flow

> Numbered start-to-finish flow showing primary persona journey through screens.

1. Persona lands on S1 (entry)
2. Clicks [CTA] → S2
3. Fills form → S3 (success)
4. ...

## 7. Dev Handoff Notes

- **Tailwind config implications:** [which tokens to add to `tailwind.config.ts`]
- **shadcn/ui components needed:** [list]
- **Custom components to build:** [list with effort estimate]
- **Asset list:** images / icons / illustrations to generate or source
- **Accessibility checklist:** WCAG 2.1 AA, contrast ratios, keyboard nav, screen reader

## 8. Prototype/Implementation Reference

- `.pen` file: `designs/<slug>.pen` (if Pencil mode used)
- Tokens machine-readable: `design-system/tokens.json` (auto-derived from §2)
- Prototype scaffold: `prototypes/<slug>/` (after `/team-prototype`)
- Full app implementation: `src/app/**` (after `/team-vibe`)

## 9. Revision Log

| Date | Author | Change |
|---|---|---|
| YYYY-MM-DD | ui-ux-designer | Initial spec |
